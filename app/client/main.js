'use strict';

// This file is called 'main.js' to ensure it loads last
System.import('/client/ng-bootstrap').then(() => {
    console.log('Ready!');
    // Any additional logic on bootstrap finish can be put here
});
